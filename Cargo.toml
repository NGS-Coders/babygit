[package]
name = "babygit"
version = "0.1.0"
edition = "2021"

[workspace]
exclude = ["spacetimedb"]

[dependencies]
dotenv = "0.15"
spacetimedb-sdk = { version = "2.0" }
tokio = { version = "1.50", features = ["fs", "rt"] }
